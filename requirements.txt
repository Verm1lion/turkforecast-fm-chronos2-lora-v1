# TurkForecast-FM locked dependency versions (Path B, reproducibility-critical).
# Tested on Python 3.10 / CUDA 12.4 / A100-SXM4-80GB Colab Pro+.
# Tier 1.5 deterministic setup requires these exact versions.

# Pins critical for INFERENCE numerical bit-equivalence (Chronos2 / LoRA / calibration math):
torch==2.4.1
torchvision==0.19.1
transformers==4.46.3
peft==0.13.2
accelerate==1.13.0
chronos-forecasting==2.2.2

# Loosened to match salesforce-gift-eval upstream pyproject.toml constraints
# (training pipeline used stricter pins; inference does not need them for output equivalence):
#   gift-eval requires: gluonts~=0.15.1, datasets~=2.17.1, numpy~=1.26.0, scipy~=1.11.3, pandas>=2.0
gluonts>=0.15.1,<0.17
datasets>=2.17,<3.0

# Standard scientific stack (gift-eval upstream constraints respected)
numpy>=1.26,<2.0
pandas>=2.0
scipy>=1.11,<1.12

# Calibration (P5.6 Kuleshov isotonic + pkl serialization)
scikit-learn>=1.5
joblib>=1.3

# HF Hub adapter download
huggingface-hub>=0.24

# GIFT-Eval data loader (for replicate.py)
# Note: PyPI package name is 'salesforce-gift-eval' (per upstream pyproject.toml);
# import name remains 'gift_eval'.
salesforce-gift-eval @ git+https://github.com/SalesforceAIResearch/gift-eval.git

# v1.1 pipeline dependency notes:
#   - peft==0.13.2: required for LoRA adapter loading (PeftModel.from_pretrained)
#   - huggingface-hub>=0.24: revision="v1.1" pin for deterministic artifact download
#   - joblib>=1.3: load Q4 v1 + Horizon-CQR + bucket isotonic PKLs
#   - scikit-learn>=1.5: IsotonicRegression unpickling
#   - scipy>=1.11: Q4 v1 Variant-B per-quantile temperature optimizer
