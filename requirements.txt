# TurkForecast-FM-Chronos2-LoRA-v1 locked dependency versions (Path B, reproducibility-critical).
# Tested on Python 3.10 / CUDA 12.4 / A100-SXM4-40GB Colab Pro+.
# Tier 1.5 deterministic setup requires these exact versions for bit-equivalent replay.

torch==2.4.1
torchvision==0.19.1
transformers==4.46.3
peft==0.13.2
accelerate==1.13.0
chronos-forecasting==2.2.2
gluonts==0.16.2
datasets==4.0.0

# Replication pipeline extras (V4 router + bucket isotonic calibrators)
huggingface_hub>=0.25
joblib>=1.3
scikit-learn>=1.5
scipy>=1.11

# Standard scientific stack (Colab defaults are compatible)
numpy>=1.26,<2.0
pandas>=2.0
