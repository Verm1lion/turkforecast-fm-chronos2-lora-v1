# TurkForecast-FM locked dependency versions (Path B, reproducibility-critical).
# Tested on Python 3.10 / CUDA 12.4 / A100-SXM4-80GB Colab Pro+.
# Tier 1.5 deterministic setup requires these exact versions.

torch==2.4.1
torchvision==0.19.1
transformers==4.46.3
peft==0.13.2
accelerate==1.13.0
chronos-forecasting==2.2.2
gluonts==0.16.2
datasets==4.0.0

# Standard scientific stack (Colab defaults are compatible)
numpy>=1.26,<2.0
pandas>=2.0
scipy>=1.11
